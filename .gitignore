# Build output
/target

# Cargo.lock is COMMITTED for binary crates (reproducible builds).
# This project is a binary (win-mouse-fix), so do not ignore it.

# Runtime logs
*.log

# Editor / OS noise
.vscode/
.idea/
Thumbs.db

# Python cache (icon generator)
__pycache__/
tools/__pycache__/

# Standalone probe binary built from src/bin/dpi-probe.rs (build artifact, not source)
/dpi-probe.exe

# Persisted pairing token (minted at first run, must stay stable across restarts)
token.txt

# Backup / artifact files dropped during development
*.old
# Playwright e2e tests (run locally, not in CI)
tests/
# Node.js dependencies (Playwright tests)
node_modules/
package.json
playwright.config.js

# Playwright test artifacts
test-results/
test-screenshots/

# Temporary debug scripts and logs
test_*.js
test-*.js
stdout*.txt
stderr*.txt
stdout.txt
stderr.txt
out.txt
output.txt
err.txt
*.log.1
*.log.2
*.log.3

# Development docs and plans
PLAN-*.md
docs/phone-*.md

# Development scripts
run_and_monitor.ps1
test_ws.ps1

# Screenshot artifacts
winlist-debug*.png
winlist-final.png

# OMC config
.omc/

# Python development files
assets/inject_winlist.py
assets/patch*.py
assets/patch_trackpad.py

# Probe/diag binaries
src/bin/thumb-dwm-poc.rs
src/bin/ws-diag.rs
