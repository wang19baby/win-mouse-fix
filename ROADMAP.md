# Win Mouse Fix — 开发路线图

> 最后更新: 2026-09-11（按运行时代码实查校正）
> 当前重点: 输入主链路稳定性 + Phase 13 手机触控板可靠性 + 真实硬件验收

---

## 代码实查状态

| Phase | 功能 | 代码与运行时状态 |
|---|---|---|
| P0 | 基础验证 | [完成] |
| P1 | 平滑滚动 | [完成] 已接入钩子；注入线程生命周期与空闲等待已修复 |
| P2 | 按键重映射 | [完成] 已接入；托盘录制入口已恢复 |
| P3 | 点击拖拽手势 | [完成] 已接入；仍需真实鼠标手感验收 |
| P4 | 修改键组合 | [完成] Shift/Ctrl 滚动路径已接入 |
| P5 | 指针加速 | [部分] 算法与状态控制器有测试，尚未接入 `WM_MOUSEMOVE` 运行时 |
| P6 | 配置 GUI + 热重载 | [部分] 托盘与 TOML 热重载可用；独立 GUI 代码尚未暴露 |
| P7 | 每应用配置文件 | [完成] 已接入；无 profile 时不再轮询进程/重装钩子 |
| P8 | 设备层（电池+DPI+枚举） | [部分] 查询代码存在，需支持设备真机验收 |
| P9 | 电池托盘图标 | [部分] 图标渲染代码存在，刷新定时器当前未启用 |
| P10 | DPI 跨屏自动切换 | [部分] 切换逻辑存在，轮询定时器当前未启用，硬件写入需真机确认 |
| P13.1 | 手机触控板基础连通 | [完成] HTTP + WS + 注入管线；启动/停止生命周期已修复 |
| P13.2 | 触控参数配置化 | [完成] `[touch]` 配置段 + WS 下发 |
| P13.3 | 滚动平滑适配 | [完成] 触摸输入直接走 `SendInput` |
| P13.5 | Web App | [部分] manifest 已有；Service Worker、离线与多设备真机验证未完成 |
| P13.8 | 语音输入 | [部分] Web Speech 路径已实现；依赖浏览器能力与真机验证 |
| P14.1 | 退出确认对话框 | [完成] |
| P14.2 | 帮助文档 | [完成] 托盘入口已接通并按实际功能校正 |
| P14.3 | 错误上报 | [完成] 本地 panic hook → crash.log |
| P15 | 日志优化 | [完成] 文件轮转；输入热路径日志已移除，认证令牌/原始输入不落盘 |
| P16.1 | G Hub 优化 | [部分] 轮询/查询代码存在；真实 G Hub 与设备组合仍需验收 |

测试总数以 `cargo test --all-targets` 的最新质量门禁为准，结果记录在 `docs/architecture-stability-optimization-evidence.md`。

---

## Phase 13 — 手机触控板（进行中）

### 13.1 基础连通性 ✅ 已完成
- [x] HTTP 服务器（嵌入式 HTML）
- [x] WebSocket 服务器（手写 RFC 6455）
- [x] Windows CSPRNG bearer 认证 + 仅主机可读的 QR 页面（传输仍为明文，仅限可信 LAN）
- [x] 防火墙规则调用路径（管理员权限与真实网络环境仍待验收）
- [x] 注入管线对接（send_mouse_move / push_remote_scroll / send_remote_click）
- [x] Anti-feedback-loop 守卫
- [x] 单元测试 + 集成测试框架

### 13.2 触控参数配置化 ✅ 已完成
- [x] 新增 `[touch]` 配置段到 `config.rs` + `config.toml`
- [x] 服务端读取 `[touch]` 配置，通过 WS status 下发给手机端
- [x] 手机端 JS 从 status 消息读取参数（替代硬编码常量）

### 13.3 滚动平滑适配 ✅ 已完成
- [x] 触摸滚动绕过 `ScrollInjector`，直接 `SendInput` wheel

### 13.4 手势体验优化
- [ ] 手势消歧逻辑优化（scroll vs pinch 的 `DECIDE_PX` 阈值）
- [ ] 3/4 指滑动灵敏度调整
- [ ] 对角线手势判定优化（`DIAG_RATIO` 参数）
- [ ] 手势视觉反馈增强（手机端 ripple + PC 端 HUD）
- [ ] 长按拖动 = 窗口拖拽（`FakeDrag` 模式）

### 13.5 PWA 完善 ✅ 已完成
- [x] 创建 `manifest.json`（名称、图标、主题色、display: standalone）
- [ ] 添加 Service Worker（离线缓存）
- [ ] iOS Safari "添加到主屏幕" 引导 UI
- [ ] 全屏模式：隐藏地址栏 + 状态栏
- [ ] 横屏锁定（可选）

### 13.6 状态下推
- [ ] 服务端 → 客户端 status 消息扩展（PC 电量、手机电量）
- [ ] 手机端 HUD 显示 PC 鼠标电量（从 `cache::BATTERY` 读取）
- [ ] 手机端显示手机电量（`navigator.getBattery()`）

### 13.7 网络鲁棒性
- [ ] 多网卡 LAN IP 枚举（`GetAdaptersAddresses`）
- [ ] 断线重连优化（指数退避 + 状态遮罩）
- [ ] WebSocket ping/pong 真机验证
- [ ] 连接质量指示（延迟/丢包率）

### 13.8 语音输入 ✅ 已完成
- [x] 协议扩展：`{"t":"key","text":"..."}` 消息类型
- [x] 服务端 handler：`send_remote_text()` 逐字符键盘注入
- [x] 手机端 UI：🎤 按钮 + Web Speech API + 输入框
- [x] 语音优先：打开输入栏自动启动语音识别

---

## Phase 16 — 设备层深化

### 16.1 G Hub 集成优化（部分）
- [x] 非侵入式端口就绪检查；G Hub 未运行时不会擅自启动
- [x] 进程启动后立即后台读取，此后每 20 秒刷新且仅记录状态变化
- [ ] 多设备缓存、热插拔及真实 G Hub 版本兼容性验证

### 16.2 按钮绑定读取 ❌ 阻塞
G Hub WebSocket `/card/{id}` 返回 `NO_SUCH_PATH`。可能路径：
- HID++ onboard memory（需进一步研究）
- 接受限制，不读取按钮绑定

### 16.3 多设备支持
- [ ] 设备列表 UI（显示所有检测到的设备）
- [ ] 按设备 ID 区分配置
- [ ] 设备热插拔检测（`WM_DEVICECHANGE`）

### 16.4 板载内存直接操作
- [ ] 研究 HID++ 板载内存 feature
- [ ] 实现板载 profile 读写
- [ ] 备份/恢复设备配置

### 16.5 按钮重映射集成
- [ ] 定义按钮 ID 映射（G1-G11 → 内部按钮码）
- [ ] GUI 中显示当前按钮绑定
- [ ] 支持修改按钮绑定（通过 RemapEngine）

---

## 建议执行顺序

| 优先级 | 任务 | 说明 |
|---|---|---|
| 高 | 13.4 手势消歧优化 | 提升触控板核心体验 |
| 高 | 13.6 状态下推 | 手机显示 PC 电量 |
| 中 | 13.7 网络鲁棒性 | 多网卡 + 断线重连 |
| 中 | 13.5 PWA Service Worker | 离线缓存 |
| 低 | 16.3 多设备支持 | 需多设备硬件 |
| 低 | 16.4 板载内存 | HID++ 研究 |
| 低 | 16.5 按钮集成 | 依赖 16.2 |

---

## 技术债务

| 项目 | 优先级 | 状态 |
|---|---|---|
| 编译警告清理 | 低 | ✅ 70 → 0 warnings |
| dpi-probe 构建错误 | 低 | 独立二进制，非关键 |
| 性能基准测试 | 中 | 滚动延迟、CPU 占用基准 |
| 测试覆盖率提升 | 中 | 集成测试需交互式会话 |
